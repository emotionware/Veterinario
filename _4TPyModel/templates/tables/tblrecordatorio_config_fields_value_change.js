<script>
$(document).ready(function () { $("#IDRECORDATORIO").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#IDRECORDATORIO").val(); 
 });}); 

 $(document).ready(function () { $("#IDPROSPECTO").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#IDPROSPECTO").val(); 
 });}); 

 $(document).ready(function () { $("#IDTIPORECORDATORIO").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#IDTIPORECORDATORIO").val(); 
 });}); 

 $(document).ready(function () { $("#FECHA").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#FECHA").val(); 
 });}); 

 $(document).ready(function () { $("#HORA").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#HORA").val(); 
 });}); 

 $(document).ready(function () { $("#DESCRIPCION").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#DESCRIPCION").val(); 
 });}); 

 $(document).ready(function () { $("#REALIZADO").on("change", 
 function (event) { 
 // var type = event.args.type; // keyboard, mouse or null depending on how the value was changed. 
   // var value = $("#REALIZADO").val(); 
 });}); 

 </script>